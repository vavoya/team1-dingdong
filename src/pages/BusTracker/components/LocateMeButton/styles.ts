import styled from "styled-components";
import {colors} from "@/styles/colors.ts";

export const ButtonWrapper = styled.button`
    fill: ${colors.gray0};
    filter: drop-shadow(0px 1px 2px rgba(34, 34, 53, 0.30));
`